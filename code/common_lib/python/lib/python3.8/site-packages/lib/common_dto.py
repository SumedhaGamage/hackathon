import json

class GeneralMessage:

    def __init__(self, *, message=None, error_code=0, **kwargs):
        self.message = message
        self.error_code = error_code

    @staticmethod
    def json_load(data):
        return GeneralMessage(**data)

    def to_json(self):
        return json.dumps(self.__dict__)
